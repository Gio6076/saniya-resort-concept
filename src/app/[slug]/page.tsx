import Link from "next/link";
import { ArrowLeft, Construction } from "lucide-react";
const labels:Record<string,string>={gallery:"Gallery"};
export default async function PlaceholderPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const label=labels[slug]??"Experience";return <section className="container-shell grid min-h-[65svh] place-items-center py-20 text-center"><div className="max-w-xl"><span className="mx-auto grid size-16 place-items-center rounded-full bg-[#dcece5] text-brand"><Construction aria-hidden="true"/></span><p className="eyebrow mt-6 text-coral">Next milestone</p><h1 className="display-title mt-4 text-5xl">{label}</h1><p className="mt-5 leading-7 text-muted">This route is intentionally staged for the next implementation milestone. The homepage foundation and navigation are ready for this experience to be built.</p><Link href="/" className="mt-8 inline-flex items-center gap-2 rounded-full bg-brand px-6 py-3 font-bold text-white"><ArrowLeft className="size-4" aria-hidden="true"/>Back to homepage</Link></div></section>}
