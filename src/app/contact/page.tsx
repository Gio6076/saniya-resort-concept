import type { Metadata } from "next";
import { Mail, MapPin, MessageCircle, Navigation, Phone, Route, type LucideIcon } from "lucide-react";
import { ContactForm } from "@/components/contact/contact-form";
import { contactDetails, contactMethods, planningQuestions } from "@/data/contact";
export const metadata: Metadata = { title: "Contact & location planning", description: "A conceptual contact and location-planning page for an independent Saniya Resort portfolio concept. It uses replaceable contact details and a local-only demo form." };
const icons: Record<string, LucideIcon> = { mail: Mail, phone: Phone, message: MessageCircle };
export default async function ContactPage({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) { const query = await searchParams; const topic = typeof query.topic === "string" ? query.topic : undefined; return <><section className="bg-coral py-14 text-white sm:py-20"><div className="container-shell"><p className="eyebrow text-white/70">Contact & location · concept planning</p><h1 className="display-title mt-4 max-w-4xl text-5xl sm:text-7xl">Start with the questions that make a trip feel easy.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-white/85">This is a planning destination, not an official contact channel. Details are replaceable placeholders, the map is intentionally unpinned, and the form stays local to your browser.</p></div></section><section className="py-16 sm:py-24"><div className="container-shell grid gap-8 lg:grid-cols-[.9fr_1.1fr]"><div><p className="eyebrow text-brand">Conceptual ways to connect</p><h2 className="display-title mt-4 text-4xl">Use a demo detail, or leave a local note.</h2><p className="mt-5 leading-7 text-muted">These details are not verified resort contact information. For a real visit, find an independently verified official business channel.</p><address className="mt-8 not-italic">{contactMethods.map(method => { const Icon = icons[method.icon]; const body = <><Icon className="mt-1 size-5 shrink-0 text-brand" aria-hidden="true" /><span><span className="block font-bold">{method.label}</span><span className="mt-1 block">{method.value}</span><span className="mt-1 block text-xs leading-5 text-muted">{method.note}</span></span></>; return <div key={method.label} className="border-b border-ink/10 py-5">{method.href ? <a className="flex gap-3 hover:text-brand" href={method.href}>{body}</a> : <div className="flex gap-3">{body}</div>}</div>; })}</address></div><div className="relative min-h-80 overflow-hidden rounded-[2.5rem] bg-ink p-7 text-white sm:p-10"><div className="absolute -right-16 -top-20 size-64 rounded-full border-[38px] border-sun/25"/><div className="absolute -bottom-20 -left-12 size-52 rounded-full bg-brand/80"/><div className="relative"><MapPin className="size-9 text-sun" aria-hidden="true"/><p className="eyebrow mt-8 text-sun">Replaceable map area</p><h2 className="mt-3 font-display text-4xl">A broad location cue, with no invented pin.</h2><p className="mt-5 max-w-md leading-7 text-white/75">{contactDetails.location} is a demo location label only. A verified Google Maps or OpenStreetMap embed can be added here once the official address and map permission are confirmed.</p><div className="mt-7 flex items-center gap-3 text-sm text-white/70"><Navigation className="size-5 text-sun" aria-hidden="true" />No address, coordinates, route time, or map embed is shown.</div></div></div></div></section><section className="bg-[#dcece5] py-16 sm:py-24"><div className="container-shell grid gap-10 lg:grid-cols-[.8fr_1.2fr]"><div><p className="eyebrow text-coral">Travel-planning guidance</p><h2 className="display-title mt-4 text-4xl">Plan the journey with verified details.</h2><p className="mt-5 leading-7 text-muted">This concept cannot give directions or travel times. Use it as a reminder of what to confirm before departure.</p><div className="mt-7 rounded-2xl bg-surface p-5"><Route className="size-6 text-brand" aria-hidden="true"/><p className="mt-3 font-bold">Before setting out</p><p className="mt-2 text-sm leading-6 text-muted">Verify the official address, transport options, route conditions, arrival instructions, accessibility needs, parking or transfers, and current policies with an authorized source.</p></div></div><div className="grid gap-3">{planningQuestions.map((item, index) => <details key={item.question} open={index === 0} className="group rounded-2xl bg-surface p-5"><summary className="cursor-pointer list-none pr-8 font-bold marker:content-none">{item.question}<span className="float-right text-brand group-open:rotate-45" aria-hidden="true">+</span></summary><p className="mt-3 text-sm leading-6 text-muted">{item.answer}</p></details>)}</div></div></section><section id="message" className="container-shell scroll-mt-24 py-16 sm:py-24"><div className="grid items-start gap-10 lg:grid-cols-[.8fr_1.2fr]"><div><p className="eyebrow text-brand">Local message preview</p><h2 className="display-title mt-4 text-4xl">Tell the planning story in your own words.</h2><p className="mt-5 leading-7 text-muted">The form asks for only the details needed to demonstrate a thoughtful inquiry flow. It does not send, save, reserve, or share anything.</p></div><ContactForm initialTopic={topic} /></div></section></>; }
