import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { ArrowRight, CircleAlert, Sparkles } from "lucide-react";
import { PackageCard } from "@/components/packages/package-card";
import { packagePricingGuidance, packages, packagesHero } from "@/data/packages";
import { createPageMetadata } from "@/lib/seo";
export const metadata: Metadata = createPageMetadata("/packages", "Packages & Illustrative Rates", "Compare conceptual day-use, overnight, family, celebration, and group package ideas in this independent Saniya Resort portfolio concept. Prices are illustrative and availability is never live.");
export default function PackagesPage() { return <><section className="overflow-hidden bg-[#f4c16a] py-14 sm:py-20"><div className="container-shell grid gap-10 lg:grid-cols-[1.1fr_.9fr] lg:items-end"><div><p className="eyebrow text-brand">Packages & rates · independent concept</p><h1 className="display-title mt-4 max-w-3xl text-5xl sm:text-7xl">Pick the kind of together-time you want to make.</h1><p className="mt-6 max-w-2xl text-lg leading-8">Explore a commercial package experience built for comparison. Every offer, inclusion, limitation, and price below is conceptual—never a confirmed resort rate or available package.</p></div><figure className="relative overflow-hidden rounded-[2rem] bg-ink text-white"><div className="relative aspect-[16/10]"><Image src={packagesHero.src} alt={packagesHero.alt} fill sizes="(max-width:1024px) 100vw,40vw" className="object-cover" /></div><figcaption className="absolute inset-x-0 bottom-0 bg-ink/75 p-5 backdrop-blur-sm"><Sparkles className="size-6 text-sun" aria-hidden="true" /><p className="mt-2 font-display text-xl">A clear starting point, not a quote.</p><p className="mt-1 text-xs leading-5 text-white/75">Concept image; use each card’s price unit to compare ideas.</p></figcaption></figure></div></section><section className="py-16 sm:py-24"><div className="container-shell"><div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="eyebrow text-coral">Five ways to plan</p><h2 className="display-title mt-3 text-4xl sm:text-5xl">Choose an outline, then shape the details.</h2></div><p className="max-w-sm text-sm leading-6 text-muted">The card layout keeps each offer readable on small screens and makes its unit, inclusions, and limits easy to find.</p></div>{packages.length ? <div className="mt-10 grid gap-6 lg:grid-cols-2">{packages.map((offer, index) => <PackageCard key={offer.id} offer={offer} featured={index === 1} />)}</div> : <div className="mt-10 rounded-[2rem] bg-surface p-8"><h2 className="font-display text-3xl">Package ideas are being refreshed.</h2><p className="mt-3 max-w-xl leading-7 text-muted">There are no demo offers to compare right now. You can still explore accommodation ideas or leave a local planning message.</p><div className="mt-6 flex flex-wrap gap-3"><Link href="/accommodations" className="rounded-full bg-brand px-5 py-3 font-bold text-white">Explore stays</Link><Link href="/contact" className="rounded-full border border-ink/20 px-5 py-3 font-bold">Contact planner</Link></div></div>}</div></section><section className="bg-ink py-16 text-white sm:py-20"><div className="container-shell grid gap-10 lg:grid-cols-[.85fr_1.15fr]"><div><p className="eyebrow text-sun">Demo-pricing guidance</p><h2 className="display-title mt-4 text-4xl">Know what the numbers mean.</h2></div><ul className="space-y-5">{packagePricingGuidance.map(item => <li key={item} className="flex gap-3 leading-7 text-white/80"><CircleAlert className="mt-1 size-5 shrink-0 text-sun" aria-hidden="true" />{item}</li>)}</ul></div></section><section className="py-16 sm:py-24"><div className="container-shell rounded-[2.5rem] bg-[#dcece5] px-6 py-12 sm:px-12"><p className="eyebrow text-coral">Keep planning</p><h2 className="display-title mt-4 max-w-2xl text-4xl sm:text-5xl">A stay, a gathering, or a question—take the next useful step.</h2><nav aria-label="Package planning links" className="mt-8 flex flex-wrap gap-3"><Link href="/booking" className="inline-flex min-h-12 items-center gap-2 rounded-full bg-ink px-6 py-3 font-bold text-white hover:bg-brand">Start a demo booking <ArrowRight className="size-4" aria-hidden="true" /></Link><Link href="/accommodations" className="inline-flex min-h-12 items-center rounded-full border border-ink/20 px-6 py-3 font-bold hover:bg-surface">Compare accommodation ideas</Link><Link href="/events#inquiry" className="inline-flex min-h-12 items-center rounded-full border border-ink/20 px-6 py-3 font-bold hover:bg-surface">Create an event brief</Link></nav></div></section></>; }
